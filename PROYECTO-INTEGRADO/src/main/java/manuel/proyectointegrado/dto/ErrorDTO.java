package manuel.proyectointegrado.dto;

//DTO que sirve para devolver el mensaje de error, RECORD basicamente es una estructura de datos con campos y es inmutable
public record ErrorDTO(String message) {
}
